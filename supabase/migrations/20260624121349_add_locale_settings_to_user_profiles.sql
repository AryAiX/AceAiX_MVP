ALTER TABLE user_profiles
  ADD COLUMN IF NOT EXISTS locale VARCHAR(10) DEFAULT 'en',
  ADD COLUMN IF NOT EXISTS timezone VARCHAR(100) DEFAULT 'UTC',
  ADD COLUMN IF NOT EXISTS unit_system VARCHAR(20) DEFAULT 'metric';
